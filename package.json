{
  "name": "appsync-events-retry",
  "version": "1.0.0",
  "description": "Service for guaranteeing payload deliveries with Web sockets using AppSync Events API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-esbuild": "^1.31.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.693.0",
    "@aws-sdk/client-scheduler": "^3.693.0",
    "@serverless-guru/logger": "^1.0.5",
    "esbuild": "^0.24.0",
    "uuid": "^11.0.3"
  }
}
